# 进度记录

## 2026-07-14

- 用户反馈 1.2.0 安装版存在三类回归：鼠标注视未生效且眼睛附近出现两个深蓝色圆点；宠物随时间或拖拽后自动放大；可能还有未发现问题。本轮将以安装版真实交互而非单帧截图为验收标准。
- 已定位当前安装版进程、用户数据目录和 150% 显示缩放环境；初步确认深蓝圆点是额外 CSS 瞳孔层，尺寸变化更可能来自动作图主体比例及拖拽强制切换姿势，仍需用实际边界测量确认。
- 已确认用户实际运行的 1.2.0 与仓库最终 `win-unpacked` 构建时间一致，设置缩放固定为 80%；ASAR 首次逐文件读取因归档路径分隔符错误中断，随后列包确认文件完整，下一步改用正确路径继续比对。
- 已量化全部动作图透明主体边界：最窄 66.8%、最宽 95.4%，拖拽由待机切到害羞会造成约 30% 的横向视觉放大；动作总览视觉复核与数据一致。
- 已暂时停止安装版进程并用发布目录实测中心、东、西注视及害羞姿势；确认鼠标方向 IPC 正常，但注视位移仅约 2 个物理像素且额外瞳孔无眼眶裁剪，属于渲染设计缺陷。
- 已评估复用 Codex v2 的 16 方向图格；单格只有 `192x208`，不满足当前 Electron 高清显示需求，决定保留高清动作素材并重做无气泡的方向反馈。
- 已完成 7 个动态眼睛动作在浅色背景、东南极端方向下的安装版同屏检查；确认方向反馈肉眼不可辨、动作主体大小明显跳变，继续硬编码瞳孔坐标不具备稳定性。
- 已提取并检查 v2 全部 16 个方向：方向语义清楚，但单格放大清晰度不足；桌面版将参考其方向幅度实现高清主体的无圆点注视反馈。
- 首轮 24 秒原生窗口采样读到异常尺寸波动，但采样未固定主进程 PID 且窗口正在自动散步；已盘点进程树，下一轮按固定主 PID、固定句柄和 DPI 复测后再下结论。
- 第二轮固定 PID 采样发现后台启动使用了 `-WindowStyle Hidden`，主进程 `MainWindowHandle` 为 0，20 次 Win32 API 调用均明确失败；该轮没有伪装成有效验证，下一轮改为正常显示并动态读取固定 PID 的句柄。
- 第三轮正常显示窗口后成功锁定 PID、句柄和 144 DPI：自动散步期间窗口宽度由 `243` 连续增长至 `387`，高度保持 `314`；已确认 `setPosition` 是散步与拖拽共享的真实尺寸膨胀入口。
- 已将散步、拖拽和屏幕边界修正统一改为固定宽高的 `setBounds`；移除独立瞳孔/眼睑层，改用高清主体的平滑全方向转向，并加入逐动作视觉面积校准与无缩放关键帧。
- 语法检查和 10 项测试全部通过；新增回归覆盖污染尺寸恢复、禁止任何 `setPosition` 移动路径以及禁止重新加入脱离素材的瞳孔层。
- 已完成修复后中心与八方向源码截图：圆点问题消失、方向变换生效；静态左右差异仍不够醒目，进入第二轮幅度校准。
- 第二轮九方向截图确认倾身幅度足够，但为满足“注视”语义，决定补充眼眶裁剪的实际虹膜位移，并保留倾身作为辅助方向反馈。
- 已接入眼眶裁剪虹膜层并再次通过 10 项测试；批量九方向截图成功，但同批 7 动作截图全部意外显示待机姿势，因此动作视觉结果作废，后续改为逐个确认独立进程退出后再验证。
- 已生成并检查九方向眼部放大图：中心、八方向虹膜位移清楚且均在眼眶内，注视与气泡问题的视觉修复通过待机姿势检查。
- 单独复测“开心”仍未触发，已定位启动预览钩子的 IPC 加载竞态；这属于审计发现的额外问题，将在继续动作截图前一并修复。
- 将钩子迁移到 `pet:ready` 后，语法与 10 项测试通过，但单动作进程 30 秒未生成截图也未退出；进程树仍完整，说明渲染器未完成 ready 握手或主进程未收到事件，下一步改用 DevTools 协议读取真实运行时错误。
- DevTools 已确认 preload 正常、页面完成加载，但渲染脚本未执行初始化，导致 runtime 保持 browser、ready 握手缺失；正在抓取 Chromium 脚本异常。
- DevTools 已捕获根因异常：contextBridge 的 `window.petApi` 与渲染脚本顶层 `const petApi` 冲突，安装版脚本从未运行。将本地桥接变量改名并新增真实 Electron 握手验证。
- 已将本地变量改为 `desktopBridge`，10 项测试通过且 Electron 截图可正常退出；初始对白恢复。继续修复 ready 回包被加载状态保护误丢弃的问题。
- 已修复 ready 回包路径；语法检查和 10 项测试继续通过，“开心”单动作 Electron 截图正确触发、完整显示且正常退出。
- 已完成全部 12 动作的独立 Electron 浅色背景截图与同屏检查；动作正确、角色完整、无眼眶外圆点，视觉体量校准通过。
- 已完成 24 秒真实自动散步窗口采样：144 DPI 下位置持续变化，宽高 24/24 次保持 `240x312`。
- 首次系统鼠标注入未实际触发拖拽，已判为无效验证；下一步使用 DevTools 页面事件复测拖拽状态与窗口边界。
- 已完成 DevTools 页面拖拽与 Win32 边界联合验证：拖拽状态真实进入并退出，位置按 X/Y 移动，宽高全程保持 `240x312`。
- 已新增 `npm run test:electron` 真实 Electron 冒烟测试，使用隔离临时用户目录并依赖 `pet:ready` 生成截图；当前输出 `450x585`，检查通过。
- README 已补充通用 Electron 冒烟测试命令且未写固定发行版本；项目补丁版本已提升为 `1.2.1`，源码、锁文件和根包版本一致。
- `1.2.1` 的语法检查、10 项单元测试、真实 Electron 冒烟测试和官方 npm 审计均通过，依赖漏洞为 0。
- Electron Builder 26.6.0 已成功生成 `codex-deepseek-pet-Setup-1.2.1-x64.exe` 与新的 `win-unpacked`；构建仅出现 electron-builder 内部 `shell: true` 的 Node 弃用提示，未影响产物。
- 已核对打包文件版本、ASAR 核心源码哈希、安装包签名状态和 SHA-256；打包版开心/东南注视截图通过。
- 打包版首次尺寸采样因 PowerShell 紧凑参数解析失败，应用没有启动，0 尺寸结果已明确作废；正在用正确命令重跑。
- 打包版自动散步尺寸已重跑通过：18 秒内固定句柄、144 DPI，18/18 个样本保持 `300x390`。
- 打包版真实拖拽和双击交互通过：拖拽位置变化且尺寸恒定，双击正确触发跳跃动作与对白。
- 已将新安装包静默覆盖安装到 `D:\Tools\codex-deepseek-pet`；安装目录 `app.asar` 与打包目录哈希一致，程序文件版本为 `1.2.1.0`。
- 已对实际安装版执行 Windows 全局鼠标 16 方向巡检：在 150% DPI 下校准系统坐标后，主进程坐标、渲染方向、连续 CSS 偏移和虹膜实际位移 `16/16` 全部通过。
- 已完成安装版中心与八方向九宫格及眼部放大视觉检查；方向变化清楚、虹膜始终位于眼眶内，没有两个深蓝气泡，角色与鲸尾无裁切。
- 已提交并推送运行时修复 `536ed40`，随后增加基于标签的 Windows Release 自动构建流程。
- 首次标签工作流因 electron-builder 在标签环境隐式发布且构建步骤没有 `GH_TOKEN` 而失败；安装包本身已生成。已显式设置 `--publish never`，再由带 GitHub 临时令牌的独立步骤上传。
- 受控重试工作流 `29312708577` 最终状态为 `Success`，`v1.2.1` 已发布为最新 GitHub Release，安装程序直链返回 HTTP 200。
- 已使用内置 `hatch-pet` 技能完成“蓝色大肥鱼”Codex v2 自定义宠物，补齐全部 9 行标准动作和 16 个顺时针注视方向。
- 最终图集为 `1536x2288`、8 列 11 行、`spriteVersionNumber: 2`；透明残留、洋红背景、单元边缘和结构验证均为零错误。
- 三份隔离方向盲测的四个基准方向全部通过；`157.5` 与 `202.5` 仅存在已记录的近垂直水平分量轻微歧义，带标签顺时针总览和独立最终视觉检查通过。
- 已将可直接导入的 `pet.json` 与 `spritesheet.webp` 发布到仓库目录 `codex-deepseek-pet/`，图集 SHA-256 为 `1311AF5174CC94D22FE46198C53024B95689E37036323A010D8971F3B663A636`。

## 2026-07-13

- 已通过 SSH 确认目标 GitHub 仓库可访问，并完成首版推送。
- 已检查全部参考图片：七张动作透明图均为 `384x416`，适合稳定切换。
- 已实现 Electron 主进程、安全 preload 桥接、设置持久化、系统级鼠标轮询、九区域注视、自动散步、拖拽、托盘控制和渲染层动作状态机。
- 已补充方向计算、窗口边界和设置校验测试；首版语法检查与六项测试全部通过。
- 已完成 Windows 图标、中文 README、源码与图片授权拆分、安装包配置和运行预览。
- 已对正面姿势的眼睛坐标进行放大校准；西北、东南和动作截图中的瞳孔均位于虹膜内。
- 已使用 Electron Builder 26.6.0 生成 Windows x64 NSIS 安装程序，并完成打包后截图冒烟测试。
- 已通过官方 npm 仓库完成依赖审计，结果为零漏洞。
- 首版提交为 `497e666`（`feat: build animated desktop pet`），远程 `main` 已指向该提交。
- 已开始第二轮优化：降低默认显示尺寸、修改初始对白、提升素材清晰度并补充 Codex 自定义宠物导入能力。
- 已从本机 Codex 26.707.8168.0 安装包确认自定义宠物使用本地清单，并从内置 `hatch-pet` 契约确认 v2 精灵表与 16 个注视方向要求。
- Codex 公开手册辅助脚本本次返回 403；导入格式因此以本机当前版本和随应用安装的 v2 契约为准。
- 已将默认窗口从 `360x460` 调整为 `300x390`，现有用户设置无需迁移即可获得更紧凑的初始尺寸。
- 已将初始对白改为“我不是吃白饭的大肥鱼！”，并通过 Electron 实际截图确认文案完整显示。
- 七张动作素材已统一为 `768x832` 透明画布；待机图采用角色一致的高清重绘，其透明角和可见范围已通过检查。
- 已取消斜向注视时的整图小数像素旋转，改由独立瞳孔层完成九方向注视，避免 Chromium 重采样导致主体变糊。
- 已检查中心、西北和东南方向截图：主体清晰、气泡无溢出、注视方向可辨、透明边缘无明显色块。
- 已将任务计划、调研记录、进度记录和图片素材说明改为中文，并准备在本轮纳入版本库。
- 已将版本提升到 `1.1.1`，重新生成 `codex-deepseek-pet` Windows x64 NSIS 安装程序，并通过打包后东南方向截图冒烟测试。
- 最终 1.1.1 安装程序大小为 `106268512` 字节，SHA-256 为 `4AD570ADF8371A1404747E148FEEAFE2ED60173BE199F40C4BB10573AE2DFC32`。
- 已再次执行语法检查、六项单元测试和官方 npm 安全审计，结果均通过且依赖漏洞为零。
- Codex v2 包已完成并验证待机、左右移动、挥手和跳跃动作行；每行帧提取与组件检查均为零错误、零警告。
- “等待用户”动作首轮因图片生成接口连续两次网络错误未产出；“失败”动作首轮因尾巴触边和嘴部异常被拒绝。
- “等待用户”重试后通过零警告检查；“失败”整行重做后也通过检查。
- 待机、思考和兴奋三个常用桌宠姿势已使用高清重绘，并分别完成透明角、可见范围和 Electron 实际截图检查。
- 用户要求再次重试高清“开心”“害羞”和 v2“审阅”：三项仍因内置图片接口网络错误未产生文件，未切换到需要 `OPENAI_API_KEY` 的 CLI 备用方案；现有开心与害羞表情继续保留。
- v2 中间结果留在已忽略的 `tmp/` 目录；由于“审阅”行和后续 16 方向尚未完成，不发布不合格的半成品宠物包。
- 已将核心灵感图重命名为 `assets/你这吃白饭的蓝色大肥鱼.png`（`1254x1254` PNG），并放在 README 的第一张项目介绍图位置。
- 已创建提交 `a229b1f`（`feat: sharpen pet visuals and localize docs`）并推送；GitHub 将旧仓库地址重定向到 `git@github.com:YunYueSama/codex-deepseek-pet.git`，本地 `origin` 已同步更新。
- 已开始第三轮动作素材升级；工作区初始状态干净，逐张视觉检查确认开心、害羞和左右跑步四张素材仍是低清放大图。
- 已确定重做上述四张素材，并新增挥手、审阅、困倦、跳跃和惊讶五个可触发动作。
- 本轮首次重试高清开心素材时，内置图片生成服务再次在编辑请求阶段返回网络错误，未产生文件；开始检查此前已通过验证的本地 v2 动作源作为可复用素材。
- 纯文本生成端点成功产出高清开心与害羞候选；高清跑步候选生成时再次遇到网络错误，改为复用已验证动作条并严格按单帧源尺寸筛选。
- 已确认挥手、跳跃、等待和待机动作条可提取高清桌宠姿势；失败动作单帧过小、思考动作不具备审阅语义，均不作错误复用。
- 已通过纯文本生成端点成功获得高清桌宠“审阅”单图；空白夹板、专注表情、四肢和鲸尾均完整。Codex v2 所需的六帧审阅动作行仍未完成。
- 已用连通组件逻辑重新提取挥手、惊讶、跳跃、睡眠和左右跑步，拒绝了会切断鲸尾的均分裁切版本。
- 已将动作素材扩展到 12 张，并将挥手、惊讶、审阅、跳跃和独立睡眠姿势接入随机、点击、双击和长时间空闲流程。
- 已完成九种受影响动作的 Electron 实际截图检查：人物、鲸尾和气泡完整，左右跑步方向正确，无明显洋红残边。
- 已校准挥手与惊讶姿势的瞳孔坐标，并通过西北、东南极端方向截图确认瞳孔均位于虹膜内。
- 已新增素材契约测试，自动检查 12 张运行时姿势的文件存在性、最小原生尺寸和 PNG 透明通道。
- 已将版本提升到 `1.2.0`；语法检查、7 项自动化测试和官方 npm 安全审计全部通过，依赖漏洞为零。
- 已生成 Windows x64 NSIS 安装程序并完成打包后“审阅”动作截图；最终文件大小为 `109167362` 字节，SHA-256 为 `6E20D2995E15EB0829466FAFA46AA4CD6AFF86E5904A61A87F31026E60620689`。
- 用户复核后拒绝当前开心、害羞和审阅素材：三张虽清晰完整，但与上一轮基准画风存在可见差异；当前 `1.2.0` 安装包因此视为未通过，不作为最终交付。
- 用户明确 `assets/你这吃白饭的蓝色大肥鱼.png` 是项目最重要的核心画风参考；已立即终止未将该图置于第一参考位的生成任务，未采用其结果。
- 以核心图为第一参考、现有待机图仅作姿势参考的内置图片编辑请求仍因网络错误失败；当前需要获准切换到要求本机 `OPENAI_API_KEY` 的 CLI/API 备用路径，才能继续进行严格的参考图驱动统一。
- 用户已授权 CLI/API 备用路径；检查确认进程、Windows 用户级/系统级环境变量和项目 `.env` 均未配置 `OPENAI_API_KEY`。已将 `.env` 防误提交规则加入 `.gitignore`，等待本机安全配置密钥后继续。
- 无 Key 方案改用官方 Real-ESRGAN 便携版处理同批原始素材；首次尝试 `realesrnet-x4plus` 时 PowerShell 路径函数缺少空格，模型未启动且未覆盖候选文件，已改写命令。
- 用户确认没有 `OPENAI_API_KEY`；本项目运行并不需要该密钥，因此停止等待 API 备用路径，继续使用仓库内已有高清动作源完成统一。
- 已比较原图直缩、直接超分、透明通道感知超分和孤立像素清理版本；超分只能改善锐度，无法修正旧版人物比例，全部候选均未进入正式素材。
- 已将开心替换为同批清晰笑脸姿势、害羞替换为同批合掌帧、审阅保留同批专注帧；12 动作同屏检查未再出现异画风的细线长身候选或低清粗线旧图。
- 已完成默认文案、开心与害羞西北/东南注视、审阅动作和全动作总览的 Electron 实际截图检查；人物、气泡、鲸尾、动态瞳孔和透明边缘显示正常。
- 当前语法检查与 7 项自动化测试通过；最终安装包将在素材锁定后重新构建，旧的 `1.2.0` 安装包继续视为无效候选。
- 已重新构建 Windows x64 NSIS 安装程序，并直接运行打包目录完成默认界面与开心动作冒烟检查；最终安装包大小为 `105761174` 字节，SHA-256 为 `F50D4A4877B780B8214FEA6E7980424CB08AA78E95A755FE16F0172B699AA628`。
- 最终语法检查、7 项自动化测试、官方 npm 安全审计、`git diff --check`、12 动作视觉检查和打包后截图检查全部通过。
- 已扩充 README 视觉展示：新增西北、东南两张实际窗口注视截图，并用正式透明素材展示全部 12 个动作设计；16 个 README 本地图片引用均已检查存在。
- 已修复普通浏览器直接打开 `src/renderer/index.html` 时动态瞳孔按全屏 `vw/vh` 错位的问题：浏览器静态预览自动隐藏瞳孔层并使用空桌面桥接，Electron 模式仍保留全方向注视和完整交互。
- 已新增浏览器预览回归测试；当前 8 项自动化测试全部通过，并完成 `1280x720` 普通浏览器视觉检查与 Electron 左右极端注视截图检查。

## 2026-07-14

- 已从最终 Codex v2 图集最后两行提取 `look-00.png` 至 `look-15.png`，统一生成透明 `768x832` 运行时方向素材；16 张文件均存在透明通道且内容互不重复。
- 已新增以正北为 `000°`、顺时针每 `22.5°` 一帧的方向计算，中心死区返回中立状态；四个基准方向分别稳定映射到索引 `0`、`4`、`8`、`12`。
- 已删除 HTML/CSS 中的 `gaze-layer`、`eye-layer`、眼眶与虹膜元素，以及注视用透视、旋转和拉伸变量；运行时改为直接切换完整角色立绘。
- 已调整动作状态机：空闲时跟随 16 方向，点击表情、拖拽和行走期间保留动作姿势，动作结束后恢复最近方向。
- 已为浏览器直开模式补充页面内鼠标方向预览，并保留 Electron 的系统级全局鼠标跟踪。
- 已完成 16 张 Electron 实际方向截图、中心死区截图和“开心 + 东南方向”动作抢占检查；方向连续、完整不裁切，旧深蓝虹膜圆点未出现。
- `npm run check`、13 项 Node.js 自动化测试和 `npm run test:electron` 均已通过；README 已改为真实 16 方向说明并加入完整方向设计图。
- README 项目介绍已改为“一只会在桌面陪你工作的 DeepSeek 拟人化宠物”，并继续说明蓝发鲸鱼女仆形象与系统级 16 方向注视。
- 已构建 Windows x64 NSIS 安装程序；打包后的 `app.asar` 中 5 个运行时代码文件和 16 张方向图与工作区逐一哈希一致，打包清单版本正确。
- 已从 `win-unpacked` 逐方向生成 16 张实际窗口截图并完成同屏检查；顺序、立绘、对白、透明边缘和完整性均通过。
- 已用真实 Windows 系统光标持续锁定 16 个顺时针采样点，渲染器 `data-gaze`、方向索引和实际 PNG 文件 `16/16` 一致；首轮受实体鼠标移动与随机动作干扰的结果已作废，没有计入验收。
- 新安装程序大小为 `113236576` 字节，SHA-256 为 `CA4BD8673FEA056646369D6EA34FAA6A2D65B5C04D8B3EE48F97BD145E036CF0`；没有商业代码签名证书，签名状态仍为 `NotSigned`。
- 功能提交已按要求使用中文信息：`4404cff 功能：新增真实的 16 方向鼠标注视`，并已推送到远端 `main`。
- `v1.2.2` 标签已触发 GitHub Actions；工作流 `#4` 在干净 Windows runner 中完成依赖安装、语法检查、13 项测试、NSIS 构建和 Release 上传，最终状态为 `Success`。
- `v1.2.2` 已成为最新 GitHub Release，远程安装程序直链返回 HTTP 200；CI 产物因独立构建时间戳与本地安装包字节数略有差异，二者均来自同一提交和通过检查的 `app.asar` 内容。
